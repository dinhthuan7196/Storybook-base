export enum ACTIVITIES {
  ASSIGNMENT = 1,
  PARTICIPATION = 2,
  POP_QUIZ = 3,
  TEST = 4,
  LESSON = 5,
}

export enum Tabs {
  assigned = 'Assigned',
  // grade_boost = 'Grade boost',
  done = 'Done',
}
